# Moon vCard 项目申报书
## 基本信息
- 项目名称：Moon vCard：离线读写 vCard 联系人文本的 MoonBit 库
- 参赛者：郝丽莹
- 联系方式：13011410349
- GitHub 仓库链接：https://github.com/haol-05/moon-vcard
- 项目方向：MoonBit 数据交换与内容处理基础库 / 联系人数据离线读写
- 是否为移植项目：否
## 项目简介
Moon vCard 用纯 MoonBit 实现了一个不带第三方依赖的 vCard 文本核心，接口只有 `parse`、`format`、`sort_by_preference` 三个：`parse` 一次读入多张 vCard 3.0/4.0 名片并返回联系人与诊断码，`format` 把字段写回 CRLF 换行的名片文本，`sort_by_preference` 按 `PREF` 排列多个号码或邮箱。项目面向需要在 MoonBit 中处理联系人数据的库作者、工具开发者和批量脚本，覆盖三类用法：通讯录迁移工具读入旧手机导出的 `.vcf`，得到全部联系人及姓名、电话、邮箱，缺版本或缺姓名的名片不会被静默导入而是给出 `invalid-version:5` 这类诊断；网页联系人编辑器在用户改完电话后调用 `format` 生成本机可下载的名片文本，联系人数据不经过服务器；批量清理脚本处理姓名用 quoted-printable 编码的文件，由库拼接软换行、解码 `=E5=BC=A0` 这类字节并按 UTF-8 还原，再输出姓名和按 `PREF` 排好序的号码、邮箱。库不联网、不需要系统联系人权限，16 个单元测试在 wasm、wasm-gc、js 三个目标通过，已发布到 mooncakes.io。
## 核心功能范围
- 提供 `parse`、`format`、`sort_by_preference` 三个公开接口，以及 `Contact`、`Field`、`Param` 数据模型；
- 支持一份文本中的多张名片，识别 `BEGIN`、`END`、`VERSION`、`FN`、`N`、`TEL`、`EMAIL`；
- 支持 CRLF 与 LF 换行、折行展开，以及 `\n`、`\\`、`\;`、`\,` 文本转义解码；
- 结构化姓名先按未转义的分号分段再解码，避免把 `\;` 当成字段边界；
- 保留 `TEL`、`EMAIL` 行上的 `TYPE`、`PREF` 等参数，支持 `TYPE=cell,voice` 多值、`;PREF` 无值参数和引号包裹的值，`format` 按规范写回同一行；
- 支持 `ENCODING=QUOTED-PRINTABLE` 解码：拼接行尾 `=` 的软换行、解码 `=XX` 字节并按 UTF-8 还原，解码后去掉已消费的 `ENCODING` 参数；
- 提供诊断码而不丢数据，如 `invalid-escape:3`、`invalid-quoted-printable:4`、`unsupported-charset:3`、`unclosed-card`，非 UTF-8 字符集只报诊断、不猜内容；
- 提供 `format` 输出（CRLF 结尾，版本或姓名无效时返回 `None`）与 `sort_by_preference`（按 `PREF` 数值升序稳定排列，不改动传入数组）；
- 提供 16 个单元测试（wasm、wasm-gc、js 三个目标通过）、`moon run` 示例、README 可复现命令，以及 GitHub Actions 检查（`check` / `build` / `test` 覆盖全部目标，并校验 `fmt`、`info` 工作区一致性）；
- 明确不支持 base64 照片与 `VALUE=BINARY`、属性分组（`item1.TEL`）、`FN` / `N` 的参数保留，写出时不重新做 quoted-printable 编码。
## 移植或参考说明
- 原项目名称：无，本项目为原创实现，未移植或复制现有解析器代码
- 参考规范来源链接：https://www.rfc-editor.org/rfc/rfc6350 、 https://www.rfc-editor.org/rfc/rfc2426 （RFC 6350 vCard 4.0、RFC 2426 vCard 3.0，IETF 公开文档）
- 本项目许可证：MIT
与生态内既有实现相比，本项目做了以下取舍：
- mooncakes.io 上的 `angela/vcf@0.0.5` 只支持单张名片与 `VERSION:3.0`，输入含多张名片会直接报错，不处理折行与文本转义，测试仍是工具链模板自带的 `fib` / `sum`；
- 本项目面向多张名片、3.0 与 4.0、折行与转义、非致命诊断与 CRLF 序列化，并改用 MoonBit 原生包结构、类型系统与测试方式组织代码，只使用工具链自带的 `moonbitlang/core`；
- 测试联系人全部为虚构数据，仓库内不含真实个人信息。
