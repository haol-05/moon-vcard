# Moon vCard 项目申报书

**1. 项目名称**

Moon vCard：离线读写 vCard 联系人文本的 MoonBit 库

**2. 项目简介**

`.vcf` 是手机和邮件客户端交换联系人的通用格式，文件里有折行、转义，一份文件常装多张名片，按行切分容易读错姓名或电话。Moon vCard 用纯 MoonBit 实现了这个文本核心：`parse` 一次读入多张 vCard 3.0/4.0 名片，返回可用联系人与诊断码；`format` 把字段写回 CRLF 换行的名片文本；`sort_by_preference` 按 `PREF` 排列多个号码或邮箱。库不联网、不申请系统联系人权限，已发布到 mooncakes.io，16 个单元测试在 wasm、wasm-gc、js 上通过。

**3. 项目方向，通用性说明**

方向是数据交换与内容处理，做联系人数据的离线读写。

接口只用 `String` 和自身导出的 `Contact`、`Field`、`Param`，不要求宿主提供联系人接口，也不假定调用方是网页、脚本还是服务端；包内只用到工具链自带的 `moonbitlang/core`，wasm、wasm-gc、js 三个目标都已验证。它处理的是 vCard 这个通用交换格式，不绑定某家通讯录的私有结构。

**4. 预期使用场景**

- 迁移工具读取导出的名片：把旧手机的 `.vcf` 交给 `parse`，得到全部联系人及姓名、电话、邮箱；缺少版本或姓名的名片不会被静默导入，工具能从 `diagnostics` 读到 `invalid-version:5` 这类编码，提示用户手工处理。
- 网页编辑器保存修改：用户改完电话，编辑器调用 `format` 生成 CRLF 结尾的名片文本供浏览器下载；解析与生成都在本机完成，联系人数据不经过服务器。
- 清理脚本统一字段：姓名用 quoted-printable 编码的文件交给库，它拼接软换行、解码 `=E5=BC=A0` 这类字节并按 UTF-8 还原；脚本据此输出姓名和按 `PREF` 排好序的号码、邮箱。个别名片格式有错只记录诊断码，其余照常处理。

**5. 拟实现的核心功能**

- 解析多张名片：识别 `BEGIN`、`END`、`VERSION`、`FN`、`N`、`TEL`、`EMAIL`；兼容 CRLF 与 LF，展开折行，解码 `\n`、`\\`、`\;`、`\,`。
- 保留属性参数：`TEL`、`EMAIL` 行上的 `TYPE`、`PREF` 不丢，支持 `TYPE=cell,voice` 多值、`;PREF` 无值参数与引号包裹的值，`format` 写回同一行。
- 解码 quoted-printable：识别 `ENCODING=QUOTED-PRINTABLE`，按行尾 `=` 拼回续行再解 `=XX` 字节，并去掉已消费的 `ENCODING` 参数。
- 错误可诊断：错误行只产生诊断码，不丢数据，如 `invalid-escape:3`、`unsupported-charset:3`、`unclosed-card`；非 UTF-8 字符集只报诊断，不猜内容。
- 生成与排序：`format` 输出 CRLF 结尾的名片，版本或姓名无效时返回 `None`；`sort_by_preference` 按 `PREF` 数值升序稳定排列。
- 明确不做：base64 照片与 `VALUE=BINARY`、属性分组（`item1.TEL`）、`FN`/`N` 的参数保留。

**6. 是否为原创项目、移植项目或参考已有开源项目**

原创项目，未移植或复制现有解析器的代码，格式依据公开规范自行实现，许可证 MIT。2026-09-25 在 GitHub 检索 `moonbit vcard`、`vcard moonbit`、`moonbit contacts` 均无结果；mooncakes.io 上有 `angela/vcf@0.0.5`，下载源码比对后确认差异：它只支持单张名片和 `VERSION:3.0`，不处理折行与转义，`parse`、`validate` 里留着 `println` 调试输出，测试仍是模板自带的 `fib`/`sum`。测试联系人均为虚构数据。

**7. 如为移植项目，需说明参考项目名称、来源链接和许可证**

不适用，本项目为原创。参考的是两份公开规范而非代码：[RFC 6350](https://www.rfc-editor.org/rfc/rfc6350)（vCard 4.0）与 [RFC 2426](https://www.rfc-editor.org/rfc/rfc2426)（vCard 3.0），均为 IETF 公开文档。

**8. GitHub 仓库链接**

https://github.com/haol-05/moon-vcard

`main` 分支共 19 次提交：11 次是实现与文档改动，8 次是对应 PR 的合并提交；没有空提交，没有把一次改动拆成多次，也没有重复提交，提交、Issue 与 PR 都能在仓库里逐条查看。包已发布到 https://mooncakes.io/docs/haol-05/moon-vcard 。
